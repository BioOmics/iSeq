channels:
  - conda-forge
  - bioconda
dependencies:
  - pigz
  - wget
  - axel
  - aspera-cli
  - sra-tools=2.11.0 # Due to the sra-tools >= 3.0 use CMake to build, sometimes it may be downloaded failed by conda, so use sra-tools=2.11.0